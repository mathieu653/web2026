def nombre_coureurs(lst):def nombre_coureurs(lst):
        '''
        retourne le nombre de coureurs dans lst
        '''
	assert type(lst) == list
	assert all([type(obj) == str for obj in lst]) == True
	return len(lst)

##################################

def premier(classement):
	assert type(lst) == list
	assert all([type(obj) == str for obj in lst]) == True
	return lst[o]

################################

def premier(classement):
        assert type(lst) == list
        assert all([type(obj) == str for obj in lst]) == True
        return lst[len(lst - 1)

